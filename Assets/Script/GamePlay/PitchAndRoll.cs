using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PitchAndRoll : MonoBehaviour
{
    [SerializeField] private Vector2 _maxRotation; // parametrable value
    [SerializeField] private float _rotationSpeed; // parametrable value
    [SerializeField] private float _movementSpeed; // parametrable value

    private void Start()
    {
    }

    private void Update()
    {
        
    }
}